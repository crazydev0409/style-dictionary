function flattenJson(dictionary) {
  // Implement the logic to flatten the JSON structure
  // This can be similar to the flattenObject function in the previous examples
}

module.exports = {
  transform: flattenJson,
};
